import { styled } from '@mui/material'
// molecules
import { Select as SelectComponent } from '../../molecules/select'

export const Select = styled(SelectComponent)({
  color: 'tomato',
  backgroundColor: 'pink'
})
